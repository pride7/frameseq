presentation({
  title: "Partial border fixture",
  theme: "minimal-academic",
});

slide("Underline title");
text("The title uses only a bottom border; the footer uses only a top border.");

slide("中文字体");
text("中文文本必须带上 CJK 字族，否则 PowerPoint 只能自行替换。");
